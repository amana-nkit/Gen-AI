# Steps to setup a Langchain Project

## Create Virtual environment and environment variable
conda create -p venv python=3.10
create .env file

Install package: pip install -r requirements.txt

To run: streamlit run app.py
